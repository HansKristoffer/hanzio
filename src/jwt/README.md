---
description: JWT sign/verify using jose (HS256). Install `jose` alongside consumers if tree-shaking your deps; `hanset` lists it as a dependency.
---

# hanset/jwt

```ts
import { jwtSign, jwtVerify } from 'hanset/jwt'
```
